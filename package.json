{
  "name": "scientific-articles-app",
  "version": "1.0.0",
  "description": "Платформа для научных публикаций",
  "main": "database.js",
  "scripts": {
    "start": "node database.js",
    "dev": "nodemon database.js",
    "build": "echo 'No build needed' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": "18.x"
  }
}